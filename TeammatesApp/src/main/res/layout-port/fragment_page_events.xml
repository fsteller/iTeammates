<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:id="@id/fragment_portraitPage"
                android:background="@color/holo_gray_background"
                android:padding="10dip"
                tools:ignore="InconsistentLayout">

    <RelativeLayout
        android:id="@id/android:empty"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/holo_card_error">

        <TextView
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_events"
            android:textAppearance="?android:attr/textAppearanceMedium"/>

    </RelativeLayout>

    <ListView
        android:scrollbars="none"
        android:dividerHeight="1dip"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@id/list_teammates_page"
        android:choiceMode="multipleChoice"
        android:background="@drawable/holo_card_clean"
        android:layout_below="@id/android:empty"/>

</RelativeLayout>